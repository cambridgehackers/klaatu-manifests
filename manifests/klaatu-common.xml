<manifest>

<!-- Doesn't use ssh keys - read only copy -->
<remote name="cambridge" fetch="git://gitorious.org/cambridge/" />

<!-- Add the Klaatu services code -->
<project name="klaatu-services" path="external/klaatu-services" remote="cambridge" revision="master"/>

<!-- Add the klaatu tests code -->
<project name="klaatu-tests" path="external/klaatu-tests" remote="cambridge" revision="master"/>

<project name="klaatu-initscripts" path="external/klaatu-initscripts" remote="cambridge" revision="master" />

</manifest>
